# 服务器配置
PORT=4000
NODE_ENV=production

# JWT配置（生产环境请换成强随机串：openssl rand -hex 32）
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d

# 数据库（Railway Volume 挂载到 /data）
DATABASE_PATH=/data/fanfou.db

# CORS（Vercel 部署后填入实际域名，多个用逗号分隔）
CORS_ORIGIN=https://your-app.vercel.app
