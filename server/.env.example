# 服务器配置
PORT=4000
NODE_ENV=production

# JWT配置（生产环境请换成强随机串：openssl rand -hex 32）
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d

# MySQL数据库
DB_HOST=localhost
DB_PORT=3306
DB_USER=fanfou
DB_PASSWORD=your-strong-password
DB_NAME=fanfou

# CORS（多个域名用逗号分隔）
CORS_ORIGIN=http://your-ecs-ip
